import 'package:battle_words/features/keyboard/domain/letter.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Testing keyboard letter model lass', () {
    // test('letter instance has status of error if an empty string for strStatus was input', () {
    //   final letter = Letter(text: "a", strStatus: "");

    //   final expected = LetterStatus.error;
    //   final test = letter.status;

    //   expect(test, expected);
    // });

    // test("Entering a strStatus of 'unchecked' results in a letter status of unchecked", () {
    //   final letter = Letter(text: "a", strStatus: "unchecked");

    //   final expected = LetterStatus.unchecked;
    //   final test = letter.status;

    //   expect(test, expected);
    // });

    // test("Entering a strStatus of 'incomplete' results in a letter status of incomplete", () {
    //   final letter = Letter(text: "a", strStatus: "incomplete");

    //   final expected = LetterStatus.incomplete;
    //   final test = letter.status;

    //   expect(test, expected);
    // });

    // test("Entering a strStatus of 'dne' results in a letter status of dne", () {
    //   final letter = Letter(text: "a", strStatus: "dne");

    //   final expected = LetterStatus.dne;
    //   final test = letter.status;

    //   expect(test, expected);
    // });

    // test("Entering a strStatus of 'complete' results in a letter status of complete", () {
    //   final letter = Letter(text: "a", strStatus: "complete");

    //   final expected = LetterStatus.complete;
    //   final test = letter.status;

    //   expect(test, expected);
    // });
  });
}
